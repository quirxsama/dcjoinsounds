# Railway Nixpacks Configuration
# FFmpeg, Opus ve Python kurulumu için gerekli

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "ffmpeg", "libopus"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python bot.py"
